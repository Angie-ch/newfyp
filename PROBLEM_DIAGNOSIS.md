# ERA5 数据提取问题诊断

## 根本原因

您的ERA5文件按台风逐日追踪,每天的文件有**不同的空间覆盖范围**:

```
3月23日: 纬度 -0.6 to 9.9°, 经度 141.4 to 152.4°
3月24日: 纬度 0.3 to 10.8°, 经度 137.5 to 150.8°  
3月25日: 纬度 1.2 to 11.7°, 经度 133.3 to 146.1°
```

当这些文件合并时(`xr.concat(..., join='outer')`):
- 创建了包含所有坐标的大数据集
- **缺失位置被填充为NaN**

## 问题示例

生成3月24日 12:00的样本:
- 需要提取: 3月23日 12:00, 18:00, 3月24日 00:00, 06:00, 12:00 (4个过去+1个当前)
- 台风位置在3月24日是 (5.78°N, 143.86°E)
- **但3月23日的ERA5文件在这个经度只覆盖到141.4-152.4°E** ✓
- **3月23日的ERA5文件在这个纬度只覆盖到-0.6-9.9°N** ✓

实际上这个例子应该有效,但提取代码返回了全NaN!

## 真正的问题

ERA5文件有有效数据,但**提取代码在合并数据集中某些位置返回NaN**,可能是因为:

1. **多日合并导致的NaN填充**: 不同日期文件的不同空间范围
2. **坐标对齐问题**: 合并时坐标不完全对齐,导致空间选择失败
3. **时间步引用错误**: 提取的时间步可能引用了错误文件的数据

## 解决方案

**方案1 [推荐]**: 只使用当前和未来时间步,跳过需要历史数据的早期样本
- 从台风第5个时间步开始生成样本(确保有24小时ERA5历史)
- `START_IDX = 4` (跳过前4个时间步)

**方案2**: 修改ERA5加载,为每个样本单独加载所需日期的文件
- 不预先合并所有日期
- 按需加载每个样本所需的特定日期文件

**方案3**: 扩大ERA5文件的空间覆盖
- 每个日期文件增加±5°的空间边距
- 需要重新下载ERA5数据

## 当前状态

- ERA5文件本身**完全正常** (已验证,无NaN)
- 问题出在**合并多日文件后的数据提取**
- 建议先尝试方案1(最简单)

